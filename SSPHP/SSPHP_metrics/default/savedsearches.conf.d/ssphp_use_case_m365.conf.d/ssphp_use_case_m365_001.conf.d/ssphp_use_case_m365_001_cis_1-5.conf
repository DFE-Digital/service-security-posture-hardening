[ssphp_use_case_m365_001_cis_1-5]
cron_schedule = */30 * * * *
dispatch.earliest_time = 0
dispatch.latest_time = now
enableSched = 1
search = """
| `ssphp_use_case_m365_001_cis_1-5`

| eval ii1=if(isnull('onPremisesSyncEnabled'),1,0),
       ii2=if(like('assignedPlans{}.servicePlanId',"%41781fb2-bc02-4b7c-bd55-b576c07bb09d%"),1,0),
       ii3=if(like('assignedPlans{}.servicePlanId',"%eec0eb4f-6444-4f95-aba0-50c24d67f998%"),1,0),
       ii4=mvcount('assignedPlans{}.servicePlanId')
       
| stats count as ssphp.score.denominator, sum(eval(if(ii1=1 AND ii4=ii2+ii3,1,0))) as ssphp.score.numerator

| eval ssphp.score.score=floor('ssphp.score.numerator'*100/'ssphp.score.denominator')

| fields ssphp.score.score, ssphp.score.numerator, ssphp.score.denominator


``` ##################### add metadata ##################### ```
| eval SSPHP_RUN=round(now()),
       ssphp.use_case.id="m365_001_cis_1-5",

       ssphp.use_case.version.number="1.1.0",
       ssphp.use_case.version.last_changed_date="2023-11-13",
       ssphp.use_case.version.last_changed_by="Ian Pearl",
       
       ssphp.microsoft.description=coalesce('description',"-"),
       ssphp.microsoft.implementation_status=coalesce('implementationStatus',"-")

`ssphp_use_case_add_cis_metadata`

| eval ssphp.use_case.control=coalesce('ssphp.use_case.control','ssphp.cis_benchmark.control.title',"-"),
       ssphp.use_case.description=coalesce('ssphp.use_case.description','ssphp.cis_benchmark.control.description',"-")

| eval ssphp.score.color=case('ssphp.score.score'>'ssphp.score.threshold.green',"green",1==1,"red")


`ssphp_use_case_write`

"""
