[ssphp_use_case_dns_003]
cron_schedule = 60 * * * *
description = Defender detected an Alert in Azure
dispatch.earliest_time = 0
dispatch.latest_time = now
enableSched = 1
search = """
index=_internal
| timechart count span=1h
| eventstats avg(count) as avg_count
| where count<avg_count/1.25
| stats count
| eval ssphp.score=100-(count*5)
| fields ssphp.score

``` add metadata ```
| eval SSPHP_RUN=round(now()),
       ssphp.use_case.id="dns_003",
       ssphp.use_case.description="This use case tests whether Cloudtrail logging is switched on and working properly",
       ssphp.use_case.version="1.0.0",
       ssphp.use_case.last_changed.date="2023-07-10",
       ssphp.use_case.last_changed.by="Ian Pearl"
       
| makejson SSPHP_RUN, ssphp* output=_raw
| fields _raw

```| spath input=json```
| collect output_format=hec testmode=f addinfo=f index="ssphp_metrics"

"""
