[ssphp_use_case_aad_002]
definition = """
`ssphp_metrics_data_index` sourcetype="SSPHP.AAD.user" 
    [| search `ssphp_metrics_data_index` sourcetype="SSPHP.AAD.user"
    | stats max(SSPHP_RUN) as SSPHP_RUN]

| stats values(displayName) as displayName, values(givenName) as givenName, values(surname) as surname, values(isPrivileged) as isPrivileged, list("conditionalAccessPolicies{}.displayName") as conditionalAccessPolicies_displayName, list("conditionalAccessPolicies{}.state{}") as conditionalAccessPolicies_state by id

"""

iseval = 0