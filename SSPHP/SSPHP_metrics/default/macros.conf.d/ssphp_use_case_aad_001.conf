[ssphp_use_case_aad_001]
definition = """
'ssphp_metrics_data_index' sourcetype="SSPHP.AAD.user" 
    [| search 'ssphp_metrics_data_index' sourcetype="SSPHP.AAD.user"
    | stats max(SSPHP_RUN) as SSPHP_RUN
    | return SSPHP_RUN]

| stats values(displayName) as displayName, values(isPrivileged) as isPrivileged, values("assignedPlans{}.service") as assignedPlans_service, values(givenName) as givenName, values(surname) as surname```, values(*) as *``` by id

"""

iseval = 0
