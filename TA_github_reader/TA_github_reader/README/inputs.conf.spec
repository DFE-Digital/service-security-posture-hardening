[github://<name>]
org= <value>
* The GitHub orginisation name to read from

github_token= <value>
* The GitHub PAT token to use to read from GitHub

interval= <value>
* The interval to run the check at

start_by_shell= <value>
This should be set to False as the program does not need shell expansion of provided variable

run_introspection= <value>
This should be set to True as the program will configure XML streaming with the host Splunk daemon.

[azure_client://<name>]
org= <value>
* The GitHub orginisation name to read from

interval= <value>
* The interval to run the check at
